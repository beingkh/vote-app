apiVersion: v1
kind: Service
metadata:
  name: result-service
  namespace: result
  labels:
    kubernetes.io/part-of: vote-app
    kubernetes.io/app: result
    kubernetes.io/component: result
    kubernetes.io/manual: "true"
spec:
  type: NodePort
  ports:
  - name: "result-service"
    port: 8081
    targetPort: 80
    nodePort: 31001
  selector:
    kubernetes.io/part-of: vote-app
    kubernetes.io/app: result
    kubernetes.io/component: result
    kubernetes.io/manual: "true"
