apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: redis
  labels:
    kubernetes.io/part-of: vote-app
    kubernetes.io/app: redis
    kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
  selector:
    kubernetes.io/part-of: vote-app
    kubernetes.io/app: redis
    kubernetes.io/component: redis
